app:
  jwt:
    secret: "test-only-jwt-secret-must-be-at-least-32-bytes-long"
    issuer: "rail-line-backend-test"

spring:
  datasource:
    url: jdbc:h2:mem:railline;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
